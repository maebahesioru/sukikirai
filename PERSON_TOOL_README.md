# 人物追加ツール 使用方法

## 概要

`add_person_tool.py` は、ヒカマーズ好き嫌い.comの人物データ（`data/people.json`）を簡単に管理するためのGUIツールです。

## 起動方法

```powershell
python add_person_tool.py
```

## 主な機能

### 1. 人物の追加

1. **名前**と**ID**を入力（必須）
2. **タグ**（カンマ区切り）を入力（デフォルト: ヒカマー）
3. **関連人物**のIDをカンマ区切りで入力（任意）
4. **説明**を入力（デフォルト: ヒカマー）
5. 「追加」ボタンをクリック

### 2. 人物の編集 ⭐ NEW

既存の人物のタグや説明を変更できます。

#### 方法1: ダブルクリック
- 人物リストから編集したい人物をダブルクリック
- 編集モードに切り替わり、入力フィールドに情報が読み込まれます

#### 方法2: ボタンで編集
1. 人物リストから編集したい人物を選択（シングルクリック）
2. 「選択した人物を編集」ボタンをクリック
3. 編集モードに切り替わります

#### 編集モードの使い方
- **ID欄がグレーアウト**され、編集できなくなります（IDは変更不可）
- **名前、タグ、関連人物、説明**を自由に変更できます
- 「更新」ボタンで変更を保存
- 「クリア」ボタンで編集モードを解除

### 3. 人物の削除

1. 人物リストから削除したい人物を選択
2. 「選択した人物を削除」ボタンをクリック
3. 確認ダイアログで「はい」を選択

### 4. 検索機能

- 検索ボックスに文字を入力すると、名前・ID・タグでリアルタイム検索
- 検索結果の件数が表示されます

### 5. CSV一括追加

1. 「CSV一括追加」ボタンをクリック
2. CSVファイルを選択（`display_name`, `handle`列が必要）
3. チェックボックスで追加する人物を選択
4. 「選択した人物を追加」ボタンで一括追加

## 注意事項

### IDについて
- 半角英数字、ハイフン（-）、アンダースコア（_）のみ使用可能
- アンダースコアは自動的にハイフンに変換されます
- **一度追加したIDは変更できません**（編集時もID固定）

### 編集時の注意
- 編集モード中に「追加」ボタンを押すと警告が表示されます
- 誤操作を防ぐため、編集中は「更新」ボタンになります
- 編集をキャンセルする場合は「クリア」ボタンをクリック

### データのバックアップ
- 重要なデータを編集・削除する前に、`data/people.json`のバックアップをお勧めします

## トラブルシューティング

### 「IDは既に存在します」エラー
- 同じIDの人物が既に登録されています
- 別のIDを使用してください
- 既存の人物を編集したい場合は、編集機能を使用してください

### 編集モードが解除できない
- 「クリア」ボタンをクリックすると編集モードが解除されます

### リストに表示されない
- 検索ボックスに文字が入力されていないか確認
- 検索ボックスをクリアすると全員表示されます

## ショートカット

- **ダブルクリック**: 人物を編集
- **Enter（検索ボックス）**: リアルタイム検索

## 更新履歴

### v1.1 (2025-10-22)
- ✅ 人物編集機能を追加
- ✅ ダブルクリックで編集できるように改善
- ✅ 編集モード時のUI改善（ID欄がグレーアウト、ボタンが「更新」に変更）
- ✅ 編集モード時の誤操作防止機能を追加

### v1.0
- 初期リリース
- 人物の追加、削除、CSV一括追加機能

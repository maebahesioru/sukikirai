# 人物追加ツール 使い方

## 概要

`add_person_tool.py` は、`people.json` に新しい人物を簡単に追加するためのGUIツールです。

## 起動方法

### 1. 必要なパッケージのインストール

tkinterは通常Pythonに標準で含まれていますが、念のため確認：

```bash
python --version
```

Python 3.6以上が必要です。

### 2. ツールの起動

プロジェクトのルートディレクトリで以下を実行：

```bash
python add_person_tool.py
```

## 使い方

### 入力フィールド

1. **名前** (必須)
   - 人物の表示名を入力
   - 例: `十字架`

2. **ID** (必須)
   - 人物を識別するユニークなID
   - 半角英数字・ハイフン・アンダースコアのみ使用可能
   - **アンダーバー (_) は自動的にハイフン (-) に変換されます**
   - 例: `jujika`, `person-name`, `user_123`
   - 例: `user_name` → 自動で `user-name` に変換

3. **タグ** (任意)
   - カンマ区切りで複数入力可能
   - **デフォルト: `ヒカマー`**（変更・追加可能）
   - 例: `ヒカマー, 配信者, 技術系`

4. **関連人物** (任意)
   - 関連する人物のIDをカンマ区切りで入力
   - 例: `jujika, person2, person3`

5. **説明** (任意)
   - 人物の説明文を入力
   - **デフォルト: `ヒカマー`**（変更・追加可能）
   - 複数行入力可能
   - 例: `2025世代の技術系ヒカマー`

### ボタン

- **追加**: 入力した内容をpeople.jsonに保存
- **クリア**: すべての入力フィールドをクリア
- **プレビュー**: 追加されるJSON形式のデータを確認
- **CSV一括追加**: CSVファイルから複数の人物を一括追加

## CSV一括追加機能

### CSVファイル形式

以下の形式のCSVファイルに対応しています：

```csv
"display_name","handle","profile_url"
"十字架_mania","@maebahesioru2","https://x.com/maebahesioru2"
"やすふみ","@granny_gn","https://x.com/granny_gn"
```

### 必須カラム

- `display_name`: 人物の名前
- `handle`: Twitterハンドル（@付きでもOK）

### 使い方

1. **CSVファイルを選択**ボタンをクリック
2. CSVファイルを選択
3. データが表形式で表示されます
4. 各行で以下を編集可能：
   - チェックボックス: 追加するかどうか
   - 名前: 表示名
   - ID: 識別子（自動で変換済み）
   - タグ: デフォルト「ヒカマー」（編集可能）
   - 説明: デフォルト「ヒカマー」（編集可能）
5. **すべて選択** / **すべて解除** で一括操作
6. **選択した人物を追加** で追加実行

### 自動変換機能

- ハンドルの `@` を自動削除
- アンダーバー `_` を自動的にハイフン `-` に変換
- 特殊文字を除去（英数字とハイフンのみ）
- 小文字に統一

### 例

```
入力: @Hikakin_Finder
↓
ID: hikakin-finder
```

### 重複チェック

- 既存のIDと重複する場合は自動でスキップ
- 追加結果（追加件数・スキップ件数）を表示

### 既存の人物一覧

ウィンドウ下部に既存の人物が一覧表示されます。IDの重複を避けるために確認できます。

### 人物削除機能

1. 既存の人物一覧から削除したい人物をクリックして選択
2. **選択した人物を削除**ボタンをクリック
3. 確認ダイアログが表示されるので内容を確認
4. 「はい」を選択すると削除実行

**注意**: 削除した人物は復元できません。慎重に操作してください。

## 注意事項

- IDは重複できません
- 追加前に必ずプレビューで確認することをおすすめします
- people.jsonは自動でバックアップされないため、手動でバックアップを取ることを推奨します

## トラブルシューティング

### ツールが起動しない

- Pythonがインストールされているか確認
- `python add_person_tool.py` または `python3 add_person_tool.py` を試す

### people.jsonが見つからない

- ツールはプロジェクトルートの`data/people.json`を参照します
- ファイルが存在しない場合、新規作成されます

### IDが既に存在するエラー

- 既存の人物一覧で使用されているIDを確認
- 別のユニークなIDを使用してください

## 出力形式

追加されるデータは以下の形式になります：

```json
{
  "id": "person-id",
  "name": "人物名",
  "tags": ["タグ1", "タグ2"],
  "relatedPeople": ["関連ID1", "関連ID2"],
  "description": "説明文"
}
```

## 例

### 入力例

- 名前: `山田太郎`
- ID: `yamada-taro`
- タグ: `ヒカマー, エンジニア, 配信者`
- 関連人物: `jujika`
- 説明: `プログラミング配信で人気の配信者`

### 出力されるJSON

```json
{
  "id": "yamada-taro",
  "name": "山田太郎",
  "tags": ["ヒカマー", "エンジニア", "配信者"],
  "relatedPeople": ["jujika"],
  "description": "プログラミング配信で人気の配信者"
}
```

## サポート

問題が発生した場合は、以下を確認してください：

1. Pythonのバージョンが3.6以上
2. プロジェクトルートで実行しているか
3. data/ディレクトリが存在するか
4. people.jsonの書き込み権限があるか
